def sum_of_digits(n):
    while n > 9:
        res = 0

        while n > 0:
            digit = n % 10
            res += digit
            n //= 10
        n = res

    return n


if __name__ == "__main__":
    n = int(input())

    print(sum_of_digits(n))
